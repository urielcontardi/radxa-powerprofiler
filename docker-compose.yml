services:
  powerprofiler:
    build: .
    env_file: .env
    container_name: powerprofiler
    restart: "no"
    environment:
      - REPORT_JSON_PATH=/data/report.json
    ports:
      - "5000:5000"
    volumes:
      - ./data:/data
